# getdasha.com routes — one product

Last locked 2026-08-26. Worker-first. Do not Designer-publish.

Mint: `53uxQtB9pcjWvCHguz3JTTndvuKqGxhrD37EetnCpump`
Buy: `https://jup.ag/swap?sell=So11111111111111111111111111111111111111112&buy=<mint>` — never plugin.jup.ag
TG: `https://t.me/+xB7S8mIQaKFiZjRh`
Treasury: `DwpCrg5qfCMW11a9FYFsAR9ZYQUYKNhfLdnzpci7sYgb`

## Live

| Path | Status | Owner | Index | Notes |
| --- | --- | --- | --- | --- |
| `/` | 200 | Webflow HTML + Worker transform | yes | First paint `$dasha` + Chat + Buy + one culture/mint line. Leftover `class="dasha-home-lede"` dropped after CSS/JS strip (CSS never targets `.dasha-home-lede`; lede paints via `#dasha-home-lede`) — humans still saw it in view-source. Keep `#dasha-home-lede`. Leftover `class="buy-dasha"` dropped after CSS/JS strip (CSS never targets `.buy-dasha`; contract Buy paints via `.pill.primary`; JS never reads `querySelector('.buy-dasha')`) — humans still saw it in view-source. Keep `.pill.primary`. Lobby/chess/simp `.buy-dasha` stay. Document `<title>` `$dasha` (Worker overwrite; no Webflow “make the timeline stranger”). `/index.html` 308 home (raw Webflow alias kept the leftover title + VVAIFU). Share card `$dasha` / `Buy $dasha.` (Worker OG overwrite; same lobby social card). Then Chat, Simp Quiz, Faucet. GRWM after faucet. Sign in with Grok Bot after GRWM. Tape (≤5, live `$dasha` tick first) after SIWG. Quiet Crew line after Tape remount only. Full list on `/digest`. Bag is a quiet link. No chess. No homepage Compute CTA (`#compute-door` / “Try the console” stripped from the DOM, not CSS-hide only; `/compute` stays a 200). Leftover `<nav class="dasha-nav">` and `<nav class="nav wrap">` dropped from the document (not CSS-hide only). Leftover CSS-hidden home `<footer>` (Lobby / Faucet / Bounties) dropped from the document — crawlers still saw it after style/script strip. Leftover CSS-hidden home `<a class="skip-link">Skip to content</a>` dropped from the document — crawlers still saw it after style/script strip. Leftover Webflow skip target `id="content"` dropped from home hero after skip-link DOM-strip — home has no skip-link to `#content`; privacy `#dasha-page` stays. Product `#dasha-home` + `#top` stay. Leftover Webflow contract `id="token"` dropped after CSS/JS strip (JS never reads `getElementById('token')`; CSS never targets `#token`; mint COPY is `#mint` + `.copy`) — humans still saw it in view-source. `#mint` + `.copy` + Chart stay. Leftover Webflow boot (created-in-Webflow comment, generator meta, default `favicon.ico`, w-mod-js, jquery, webflow.js) dropped from the document — leftover Studio/Desk CSS comment `DashaNav stays on lobby/studio/desk` dropped with it. Leftover Webflow Commerce `window.__WEBFLOW_CURRENCY_SETTINGS` / `CommercePrice` dropped from the document after boot strip. Leftover HTML comment (`RETIRED product's mark` / `Do not reintroduce a page-level icon`) dropped from the document — crawlers still saw it after style/script strip. Leftover HTML comments (cherries-lecture / view-transition lecture `another agent is mid-rewrite` / `Dasha canonical URL`) dropped from the document — crawlers still saw them after style/script strip. Cherries SVG stays. `@view-transition` stays. Canonical link stays. Leftover Sign in menu / leftover hamburger Webflow `w-embed w-script` dropped from the document (empty wrapper still painted after style/script strip; leftover script removed `#grok-door`). Leftover product CSS Webflow `w-embed w-script` wrapper unwrapped (empty leftover chrome after style/script strip; inner `@view-transition` + x-connect.js stay). Leftover 2020 portfolio CSS lecture (`Repairs for the 2020 portfolio template` / royal-blue / 1.1:1 WCAG) dropped from live home `<style>` — humans still saw it without style/script strip. Repair `#dasha-home h1/h2/label` rules stay. Leftover `.dasha-nav{display:none!important}` dropped from the same repair `<style>` after `<nav class="dasha-nav">` was already DOM-stripped — humans still saw the hide rule in view-source. Leftover dropped-selector CSS (`.skip-link` / `footer` / `.compute` / `.poster`) dropped from the live home product `<style>` after those nodes were already DOM-stripped — humans still saw the rules in view-source. Leftover `.navlinks` CSS dropped from live home product `<style>` after wrap-nav DOM-strip. Watch price/ticker remount belt stays. Leftover CSS lecture (`Make, Play and Buy` / motion-enhancement / sparkline) dropped from the same live home `<style>` — humans still saw retired Studio lecture in view-source. `@view-transition` rules stay. Leftover wrap-nav `.navlinks` CSS dropped from the same live home product `<style>` after `<nav class="nav wrap">` was already DOM-stripped — humans still saw it in view-source. Leftover wrap-nav `.nav` / `.brand` / `.login-link` CSS dropped from the same live home product `<style>` after wrap-nav DOM-strip — humans still saw it in view-source. Leftover hero/door `.mint-lede` / `.actions` / `.door-actions` / `.door .copy-link` CSS dropped from the same live home product `<style>` after those nodes were already DOM-stripped — humans still saw it in view-source. Contract `.copy` stays. `.pill` on `#simp-door` stays. Leftover `.lobby-log` CSS dropped from live home `#dasha-mobile-scroll` after the home DOM never had `class="lobby-log"` — humans still saw it in view-source. GRWM stays. Lobby `.lobby-log` stays. Leftover `#dasha-chess iframe` CSS dropped from live home `#dasha-mobile-scroll` after the home DOM never had `#dasha-chess` — humans still saw it in view-source. Lobby `#dasha-chess` stays. Leftover home style id=`dasha-home-play` dropped after Play was never on home — humans still saw it in view-source. Faucet CSS + `#dasha-home-faucet` stay. Leftover `chess-copy` / `DashaHomeChess` JS dropped after chess-door / `#chess-copy` was already DOM-stripped — humans still saw it in view-source. Mint COPY + `DashaHomeMint` stay. Leftover `document.documentElement.lang ||= 'en'` JS dropped after `ensureHtmlLang` already sets `<html lang="en">` — humans still saw it in view-source. `html lang` stays. Leftover `window.Webflow.push` JS dropped after webflow.js was already DOM-stripped — humans still saw it in view-source. Digest remount + `/digest.json` stay. Leftover remount `querySelector('footer')` dropped after home footer was already DOM-stripped — grok-door / grwm / main insert paths stay. Leftover `#simp` hash / `?quiz` / `?challenge` client redirect JS dropped after `#simp-door` already paints and edge `/quiz` 308 + `/?challenge` 308 already fire — humans still saw it in view-source. Quiz `#simp-door` stays. Leftover Webflow `<body class="body">` dropped from the document — johns-awesome `.body` (slate/purple, Schibsted, margin-top:-13px) still painted after style/script strip. Product `body` CSS + johns-awesome stay. Product CSS + x-connect.js stay. Cherries SVG favicon stays. Product skip-links on privacy/bounties/contribute stay. Simp stays on `#simp-door` only — out of Menu/footer. No Forum word. No VVAIFU. |
| `/digest` `/digest.json` | 200 | Worker | yes `/digest` | Full tape. Share card `$dasha Tape` / live tick title or `Tick. Room. Buy.` Worker Dexscreener pair tick first (`tick` field always present). Home remount puts that as row 1. Home shows ≤5 after SIWG. In sitemap. |
| `/lobby` `/chat` | 200 | Worker slim | yes `/lobby` | One room: live chat + lasting threads + Play (chess in-room). Share card `$dasha Lobby` / `Chat. Play. Fill the jar. Buy.` `/forum` 308 into it. Leftover empty CSS-hidden `#dasha-mint-tape` dropped (crawlers still saw it after style/script strip). Leftover `.forum-play-full` CSS dropped after Full table was already DOM-stripped — humans still saw the rule in view-source. Leftover home-only `#dasha-digest-remount` dropped after remount boot is `/` only — humans still saw it in view-source. Leftover home `#dasha-mobile-scroll` dropped after polish always injected HOME GRWM CSS — humans still saw `#grwm` / `#dasha-home` in lobby view-source. Leftover `.forum-list,.forum-thread` CSS dropped after those classes were never in the lobby DOM (threads mount is `#dasha-forum` + `df-*`) — humans still saw the rule in view-source. Leftover `.forum-title` CSS dropped after that class was never in the lobby DOM (threads mount is `#dasha-forum` + `df-title`) — humans still saw the rule in view-source. Leftover `.forum-row` CSS dropped after that class was never in the lobby DOM (threads mount is `#dasha-forum` + `df-row`) — humans still saw the rule in view-source. Leftover `.forum-replies,.forum-when` CSS dropped after those classes were never in the lobby DOM (threads mount is `#dasha-forum` + `df-*`) — humans still saw the rule in view-source. Leftover `.forum-post,.forum-reply` CSS dropped after those classes were never in the lobby DOM (threads mount is `#dasha-forum` + `df-*`; keep `.lobby-line`) — humans still saw the prefixes in view-source. Leftover `.forum-meta` CSS dropped after that class was never in the lobby DOM (threads mount is `#dasha-forum` + `df-*`; keep `.lobby-meta`) — humans still saw the prefix in view-source. Leftover `.dasha-forum` CSS dropped after that class was never in the lobby DOM (threads mount is `#dasha-forum` + `df-*`) — humans still saw the rule in view-source. Leftover `.dasha-quiet` CSS dropped after that class was never in the lobby DOM (slim header is `.dasha-word` + `.buy-dasha`) — humans still saw the rule in view-source. Keep `.dasha-slim` + `.dasha-word` + `.buy-dasha`. Keep `.dasha-lobby`. Leftover `.forum-back` CSS dropped after that class was never in the lobby DOM (threads mount is `#dasha-forum` + `df-back`; keep `.forum-send`) — humans still saw the prefixes in view-source. Leftover `id="forum-play"` dropped after CSS/JS strip (JS never reads `getElementById('forum-play')`; CSS never targets `#forum-play`; Play is `class="forum-play"` + `#forum-play-go`) — humans still saw it in view-source. Leftover `class="lobby-text"` dropped after CSS/JS strip (CSS never targets `.lobby-text`; composer is `.lobby-form textarea`; keep `.lobby-form` + `.lobby-send`) — humans still saw it in view-source. Leftover `footer.dasha-foot nav` CSS dropped after footer `<nav>` was already DOM-stripped (footer is `<p>` $dasha · Buy · Bag · Telegram) — humans still saw the rule in view-source. Keep `footer.dasha-foot` + `footer.dasha-foot a` + `.buy-dasha`. Leftover mixed `.forum-form` CSS dropped after that class was never in the lobby DOM (composer is `.lobby-form`; lobby.js mounts `lobby-form`) — humans still saw the prefixes in view-source. Keep `.lobby-form`. Mixed leftover `.forum-body` / `.forum-status` stay. Home mobile-scroll + GRWM stay. `#dasha-forum` / `#forum-play-go` / `#dasha-chess` / `.lobby-log` / `/forum/tape` stay. Home remount + `/digest.json` stay. |
| `/forum` | 308 | Worker | yes | 308 → `https://www.getdasha.com/lobby` (the room). Keep `?t=` so copied thread links still open. Chat + threads + Play are one product. |
| `/chess` | 200 | Worker | yes | Play table / embed host (`?embed=1`). Not a homepage or chrome destination. API rewritten to lobby. Leftover empty `#dasha-jup` Jupiter plugin mount dropped (crawlers still saw it after style/script strip). Leftover Jupiter plugin boot JS (`hideJup` / `bootJup` / `window.Jupiter.init`) dropped after that mount was already DOM-stripped — humans still saw it in view-source. Leftover `bootJup()` call in the buy-chip handler dropped after those functions were already stripped — humans still saw it in view-source. Buy sheet + jup.ag stay. Never plugin.jup.ag. Leftover home `#dasha-mobile-scroll` dropped after polish always injected HOME GRWM CSS — humans still saw `#grwm` / `#dasha-home` in chess view-source. Leftover `.privacy` CSS dropped after that class was never in the chess DOM — humans still saw the rule in view-source. Keep `.empty` + `.identity`. Leftover `.dasha-quiet` CSS dropped after that class was never in the chess DOM (slim header is `.dasha-word` + `.buy-dasha`) — humans still saw the rule in view-source. Keep `.dasha-slim` + `.dasha-word` + `.buy-dasha`. Leftover `.panel` CSS dropped after that class was never in the chess DOM (shell is `.app` + `.gate`) — humans still saw the rule in view-source. Keep `.app` + `.gate`. Leftover `id="gate-actions"` dropped after CSS/JS strip (JS never reads `getElementById('gate-actions')`; CSS never targets `#gate-actions`; Play chrome is `class="gate-actions"` + `#gate-action`) — humans still saw it in view-source. Keep `class="gate-actions"` + `#gate-action`. Keep `.app` + `.gate`. Leftover `id="buy-share-tg"` dropped after CSS/JS strip (JS never reads `getElementById('buy-share-tg')`; CSS never targets `#buy-share-tg`; share flash is `#buy-share-x` + a TG href) — humans still saw it in view-source. Keep the TG link. Keep `#buy-share-x`. Keep `#buy-sheet`. Leftover `id="buy-sheet-fallback"` dropped after CSS/JS strip (JS never reads `getElementById('buy-sheet-fallback')`; CSS never targets `#buy-sheet-fallback`; mint COPY is `#buy-mint` inside `#buy-sheet`) — humans still saw it in view-source. Keep `#buy-mint`. Keep `#buy-sheet`. Leftover `id="leaders-panel"` dropped after CSS/JS strip (JS never reads `getElementById('leaders-panel')`; CSS never targets `#leaders-panel`; Top table is `class="leaders"` + `#leaders`) — humans still saw it in view-source. Keep `class="leaders"` + `#leaders`. Leftover `id="recent-panel"` dropped after leftover unused JS `loadLeaders` was already stripped (JS never reads `getElementById('recent-panel')`; CSS never targets `#recent-panel`; recent games list is `class="recent"` + `#recent`) — humans still saw it in view-source. Keep `class="recent"` + `#recent`. Leftover `id="promotion-title"` dropped after CSS/JS strip (JS never reads `getElementById('promotion-title')`; CSS never targets `#promotion-title`; promotion dialog is `class="promotion"` + `#promotion`; title paints via `.promotion p`) — humans still saw it in view-source. Dangling `aria-labelledby="promotion-title"` dropped with the id. Keep `class="promotion"` + `#promotion`. Keep `id="tc-3"` / `id="tc-5"` / `id="tc-10"` (JS binds via `$('tc-'+n)`). Leftover `footer.dasha-foot nav` CSS dropped after footer `<nav>` was already DOM-stripped (footer is `<p>` $dasha · Buy · Chess · Bag · Telegram) — humans still saw the rule in view-source. Keep `footer.dasha-foot` + `footer.dasha-foot a` + `.buy-dasha`. Home mobile-scroll + GRWM stay. Forum chrome ids on `/lobby` stay (`#dasha-forum`, `forum-*`, `/forum/tape`). Leftover empty `#dasha-mint-tape` dropped. Leftover unused JS `loadLeaders` / `playNow` / `flashBought` dropped after never called (humans still saw them in view-source). Keep `#leaders` + `class="leaders"` + `#gate-action`. Keep buy sheet. Leftover unused JS `casualRematch` / `nextPlay` / `playReady` / `showPlayPair` dropped after never called (humans still saw them in view-source). Keep `showCasualBar()` + `#gate-find` + `#gate-action`. Keep `hidePlayPair()`. Leftover unused tournament CSS `.tournament-meta` / `.bracket` / `.champion` / `.entrants` / `.tournament-actions` dropped after those classes were never in the chess DOM (`wantTournamentChrome()` is false; static markup is `#tournament` + `.tournament-form`) — humans still saw leftover tournament essay CSS in view-source. Keep `#tournament` + `.tournament-form`. Leftover unused JS className strings `'tournament-meta'` / `'tournament-actions'` / `'entrants'` / `'bracket'` / `'champion'` dropped after CSS drop (`wantTournamentChrome()` is false; `renderTournament` / `renderChallenge` still run for the hidden form path so they stay; leftover className cluster never paints) — humans still saw leftover tournament className strings in view-source. Keep `#tournament` + `.tournament-form`. |
| `/chess/*` | JSON | Worker → lobby DO | no | www GET/POST proxied to the lobby Durable Object. |
| `/faucet` | 200 | Worker | yes | Typeform click-through. Once a day. Fill the jar is a side door. Share card `Fill the jar` / `Claim. Fill. Buy.` Quiet verified-fill tape, not first paint. Tape rows link to `/faucet/fill/<sig>`. Leftover view-source `dasha-home-faucet` id dropped from the tape script (home mount stays). |
| `/faucet/tape` `/faucet/fills` | JSON | Worker / faucet DO | no | GET newest-first verified fills, cap 50. CORS `*`. Empty tape is honest. Same inspect as POST `/faucet/donate`. |
| `/faucet/status` `/faucet/me` | JSON | faucet DO | no | Public GET on www + lobby. Same JSON. CORS `*`. Claim POST / SIWS / X stay lobby. `/faucet` HTML stays Typeform. |
| `/faucet/fill/:sig` `/faucet/fills/:sig` | 200 / 308 | faucet DO | no | Known fill → amount (or `in.`) + one acid `Get 100` → `/faucet`. Header `$dasha` + Buy. Quiet Solscan leftover. OG amount / `Claim. Fill. Buy.` Unknown / bad sig → 308 `/faucet`. `x-dasha-edge: faucet-fill`. |
| `/bag` | 200 | Worker | yes | Health facts on the page (mint-dead, freeze-dead, burned Raydium LP). Quiet paste under that. Default OG `$dasha · hers` / `Buy $dasha.` `?mint=` is a shareable verdict page (OG + Copy link). Default view is hers. |
| `/bag/api/record` | JSON | Worker | no | GET `?mint=` or POST `{mint}`. Hers / other-dasha / on-record / neither / unknown. CORS `*`. Worker fetch Ansem ~2.5s. Browser never calls ansem.io. |
| `/simp` | 200 | Worker / Webflow | yes | Board. Top ten + Show more. Leftover `class="dasha-quiz"` dropped after CSS/JS strip (CSS never targets `.dasha-quiz`; quiz paints via `#dasha-quiz`; simp-board.js mounts `#dasha-simp-board`) — humans still saw it in view-source. Keep `#dasha-quiz` + skip-link. Keep `#dasha-simp-board` + `.simp-quiz-go`. |
| `/how-to-buy` | 200 | Worker | yes | Exact mint. Jupiter. Share card `How to buy $dasha` / `Buy $dasha.` Leftover disclaimer `Review the route there before confirming.` dropped (crawlers still saw it after style/script strip). Leftover when-lecture `Read from the Solana mint account on 18 August 2026 at finalized commitment.` dropped (crawlers still saw it after style/script strip). Leftover `.risk` / `.facts .when` / `.facts .fine` CSS dropped after those nodes were already DOM-stripped — humans still saw the rules in view-source. Skip-link + `.actions` stay. On-chain facts stay. Buy on Jupiter stays. Leftover `id="buy2"` dropped after CSS/JS strip (JS never reads `getElementById('buy2')`; CSS never targets `#buy2`) — humans still saw it in view-source. Buy on Jupiter + `#ca` stay. `id="copy"` stays. |
| `/privacy` | 200 | Worker `PRIVACY_HTML` | yes | H1 Privacy. OAuth, wallet, Grok Bot code, cookies, faucet, board, chess. Leftover `.cta` CSS dropped after the CTA node was already DOM-stripped — humans still saw the rule in view-source. Product skip-link stays. Contribute `.cta` stays. |
| `/compute` | 200 | Worker `COMPUTE_PAGE_HTML` | yes | Use / Provide / Night / Build / Sponsor. Share card `Dasha Compute` / `Use. Provide. Night. Build. Sponsor.` Leftover meta lecture `Sponsor a Mac for the getdasha.com compute network` dropped (crawlers still saw it in view-source). Leftover `id="code-python"` / `id="code-javascript"` dropped after CSS/JS strip (JS never reads those ids; clicks are `[data-code]`; CSS never targets `#code-python` / `#code-javascript`) — humans still saw them in view-source. Keep `#code-curl` (`aria-labelledby`). Keep `data-code` + `#code`. Leftover empty `#request-json` / `#code` dropped after `paintRequest()` / `paintCode()` overwrite immediately from form defaults with no probe — humans still saw empty tags in view-source. Keep the ids + paint overwrite + `#code-curl`. Leftover stale `#count` `0 characters · nothing sent` dropped after `paintRequest()` overwrites immediately from form defaults with no probe — humans still saw it in view-source. Keep the id + paint overwrite. First-paint `63 / 2,000 characters · 0 turns kept · Enter to run · Shift+Enter for a new line`. Leftover stale `#route-note` `Hosted: GPT-OSS 20B · Community: prompts are visible to the Mac operator.` dropped after `updateRun()` overwrites immediately from hosted engine default with no probe — humans still saw it in view-source. Keep the id + `updateRun` overwrite. First-paint `Hosted GPT-OSS 20B · 3 requests per 10 minutes · Cloudflare Workers AI.` Community-operator product copy in JS stays. Leftover empty `#model` `<select id="model"></select>` dropped after `MODELS.forEach` overwrites immediately from disk models with no probe — humans still saw the empty select in view-source. Keep the id + `MODELS.forEach` overwrite. First-paint options from disk MODELS with selected `qwen3-30b-a3b`. Keep empty `#chip` / `#ram` / `#night-model` leftovers. Leftover duplicate `.nav button strong{font-size:clamp(15px,1.6vw,22px)}` dropped after the later `.nav button strong{display:block;font-size:clamp(15px,1.6vw,22px);text-transform:uppercase}` already paints the tabs — humans still saw the duplicate in view-source. Keep the later rule + `@media` 19px. Keep `.nav button`. Leftover placeholder `checking` on `#destination` / `#retention` / `#visibility` dropped after `updateRun()` overwrites immediately from engine with no probe — humans still saw it in view-source. Keep the ids + `updateRun` overwrite. Leftover empty Provide-tab mounts `#model-count` / `#tokens` / `#gross` / `#cost` / `#net` / `#compatible` dropped after `paintProvider()` overwrites immediately from form defaults with no probe — humans still saw empty tags in view-source. Keep the ids + `paintProvider` overwrite. Numbers are the inspectable scenario at disk form defaults, not sponsor P&L. Keep `#gateway-state` / `#provider-count` / `#top-state` checking (honest in-flight until `auth()`). Checking… hosted/login chrome stays (honest in-flight). Eight Apple slots in HTML. Mac Studio stays. `X-Dasha-Edge: compute`. Not a home door. In sitemap. lastmod 2026-08-29. |
| `/compute/api/*` | JSON | Worker `computeApi` on www + lobby | no | www productEdge + lobby entry. Status/healthz public JSON. Chat/providers via LOBBY DO / Workers AI. OpenAI leftover POST `/v1/completions` + `/v1/responses` (and slash) unauth 401 invalid API key, authed 400 not supported → POST `/v1/chat/completions` (no fake completions, no fake Mac). GET `/v1/chat/completions` 401 unauth / 405 authed JSON, not `{error:not found}`. POST `/v1/chat/completions` and POST `/v1/embeddings` stay. Page JS may call lobby; www must not html-404. |

| `/dasha-compute-open-alpha.tar.gz` | 200 | Worker ASSETS | no | Open-alpha kit. |
| `/crew` | 200 | Worker `CREW_PAGE_HTML` | yes | Five jobs. You keep the keys. OG + Copy prompt. `?job=scout` title Scout · Dasha Crew. `X-Dasha-Edge: crew`. Vibe line `Lobby / @dash_eats.` — no `/ simp /` product list. Simp out of footer. Quiet `/simp` stays. Leftover meta lecture `No fake P&L` dropped (crawlers still saw it in view-source). Not a home door. In sitemap. |
| `/crew/api/once` `/crew/api/log` | JSON | Worker | no | Public tape. Cache ~60s. Fail → sit. CORS `*`. |
| `/dasha-crew.tar.gz` | 200 | Worker ASSETS | no | Crew kit. |
| `/bounties` | 200 | Worker | optional | Quiet. In sitemap. GitHub contribute required. Quiet optional Connect X `#bb-x` → `/oauth/x/start?continue=1` (site-hunt X-connect; `x-connect.js` alone does not count). Leftover `.cta` CSS dropped on the empty inventory after the CTA node was already DOM-stripped — humans still saw the rule in view-source. Funded listings keep `.cta`. Product skip-link stays. Contribute `.cta` stays. |
| `/which` | 200 | Worker | yes | Quiet leftover identity. dash_eats vs VVAIFU. Share card `Which $dasha? dash_eats` / `dash_eats. Buy $dasha.` No form. Not on first paint. In sitemap. |
| `/sitemap.xml` `/robots.txt` | 200 | Worker | — | Has `/privacy` `/which` `/crew` `/digest` `/compute` `/bag` `/llms.txt` `/llms-full.txt` `/contribute` `/bounties`. lastmod 2026-08-30 on indexable 200s; `/forum` lastmod 2026-08-25 (308). No `/studio` `/dasha` `/desk` `/login` `/index.html`. No leftover 308s (`/siwg`, `/compute/use` `/provide` `/night` `/build` `/sponsor`). No `lobby?t=`. No retired rooms. `/robots.txt` is rules only (User-agent / Allow / Sitemap + one identity line). Leftover Webflow-SEO / 2026-08-08-outage / 2020-e-commerce lecture comments dropped — crawlers still saw them. |
| `/llms.txt` `/llms-full.txt` `/ai.txt` | 200 | Worker | yes | |
| `/oauth/x/start` on www | 308 | Worker | — | → `https://lobby.getdasha.com/oauth/x/start` |
| `/login` | 200 | Worker `LOGIN_PAGE_HTML` | no | Grok Bot first, then X, then wallet. `#grok` auto-starts pairing. Buy is jup.ag swap + mint. `/grok` `/siwg` 308 → `/login#grok`. HTML + HTTP Link describedby (www + lobby). |
| `/contribute` | 200 | Worker `CONTRIBUTE_HTML` | no | Open a PR. No wallet/points gate. HTML + HTTP Link describedby on www. In sitemap. Lobby host stays 404. |
| `/.well-known/grok-bot.json` | 200 | Worker | — | CORS `*`. Machine-readable SIWG on www + lobby. Path-only Worker routes `getdasha.com/.well-known/grok-bot.json*` and `getdasha.com/.well-known/*` (do not add `getdasha.com/*`). Live apex still Webflow 404 `Invalid .well-known request` unless CF starts honoring the path-only route. Next: CF Single Redirect 308 to www for ONLY that path. |
| `/auth/grok/start` `/status` `/verify` | JSON | lobby DO | no | Device-code pairing. Verify is CORS `*`. Not published yet. |
| `/client/sign-in-with-grok-bot.jpg` | 200 | Worker ASSETS | no | Ray button art for OG/share. Live control is CSS. |

## Retired

| Path | Status | Dest |
| --- | --- | --- |
| `/dasha` `/desk` `/howto` `/howtobuy` `/buy` `/purchase` `/sell` | 308 | `https://www.getdasha.com/how-to-buy` |
| `/verify` `/ca` | 308 | `https://www.getdasha.com/which` |
| `/studio` `/verse` `/learn` `/graph` `/index.html` `/candles` | 308 | `https://www.getdasha.com/` |
| `/sim` | 308 | `https://www.getdasha.com/simp` |
| `/grok` `/siwg` | 308 | `https://www.getdasha.com/login#grok` |
| `/compute/use` `/compute/provide` `/compute/night` `/compute/build` `/compute/sponsor` `/run` `/ollama` `/compute/run` `/compute/ollama` `/models` `/model` `/compute/models` `/compute/model` `/usdc` `/settle` `/topup` `/top-up` `/billing` `/wallet` `/phantom` `/solana` `/compute/usdc` `/compute/settle` `/compute/topup` `/compute/top-up` `/compute/billing` `/compute/wallet` `/compute/phantom` `/compute/solana` `/hosted` `/community` `/mixture` `/compute/hosted` `/compute/community` `/compute/mixture` `/hosts` `/inferences` `/key` `/keys` `/apikey` `/api-key` `/api_key` `/install` `/doctor` `/me` `/usage` `/inference` `/gpu` `/gpus` `/pricing` `/providing` `/mac-kit` `/fleet` `/rent` `/capacity` `/offer` `/offers` `/worker` `/workers` `/node` `/nodes` `/cluster` `/pool` `/machines` `/benchmark` `/queue` `/dashboard` `/console` `/balance` `/pay-usdc` `/apple-silicon` `/macos` `/silicon` `/local` `/edge` `/onboard` `/setup` `/quickstart` `/playground` `/sandbox` `/hello` `/example` `/examples` `/prefer` `/preference` `/preferences` `/settlement` `/settlements` `/invoice` `/invoices` `/credit` `/refill` `/kits` `/try` `/getting-started` `/get-started` `/getstarted` `/mac_kit` `/chatgpt` `/cursor` `/copilot` + `/compute/*` peers | 308 | `https://www.getdasha.com/compute` |
| `/tips` `/compute/tips` `/donate` `/once-a-day` `/once_a_day` | 308 | `https://www.getdasha.com/faucet` |
| `/job` `/compute/job` `/api/job` | 308 | `https://www.getdasha.com/compute/api/jobs` |
| `/receipt` `/receipts` `/compute/receipt` `/compute/receipts` `/api/receipt` `/api/receipts` | 308 | `https://www.getdasha.com/compute/api/receipts` |
| `/api/keys` | 308 | `https://www.getdasha.com/compute/api/keys` |
| `/gateway` `/compute/gateway` `/endpoint` `/endpoints` `/sdk` `/cli` + `/compute/*` peers | 308 | `https://www.getdasha.com/compute/api` |

Do not restore Studio. Do not list retired rooms or leftover 308s (`/siwg`, compute tab paths) in sitemap, README, or chrome as 200s. `/compute` stays the 200 product page; tab leftovers 308 there with no hash. `/compute/api` stays JSON.

## Not yet

- Drop 01 — no checkout, no prices, no images. Do not publish a door.
- `/stills` — first-party media room. GRWM already on lobby/R2, not twimg.
- Phantom exact-mint deep link — after this reliability pass. Jupiter stays the fallback.

## Tests that must stay green

Home transform: body, Buy, jup.ag, chat-door, simp-door, faucet, grwm, quiet /bag, no bag-door, no chess-door, no leftover `<nav class="dasha-nav">` or `<nav class="nav wrap">`, no `#compute-door` element, Simp out of Menu/footer, no Forum word, no plugin.jup.ag, no VVAIFU. Tape `#dasha-digest` after `#grwm` / `#grok-door`, not on first paint. Cap 5. HEAD remount `dasha-digest-remount` paints `/digest.json` after Webflow. `/digest` 200 full list.
Home OG (`dasha-home-og.test.mjs`): Worker home share card `$dasha` / `Buy $dasha.`; Webflow leftover replaced; first paint still `$dasha` + Chat + Buy; `/compute` OG `Dasha Compute` / `Use. Provide. Night. Build. Sponsor.`
Home title (`dasha-home-title.test.mjs`): document `<title>` `$dasha`; Webflow/AEO leftover rewritten; first paint + OG intact.
Ship-gate: live 308s, faucet SRI, Typeform door.
Faucet: jar empty only when status says funded false and no tokens. Missing status is not empty.
Faucet tape (`dasha-faucet-tape.test.mjs`): empty tape; one mocked donate appears; dup sig does not double-count; junk sig does not appear.
Faucet fill share (`dasha-faucet-fill-share.test.mjs`): unknown sig 308 `/faucet`; seeded fill 200 OG + jup.ag/tokens; empty tape stays `{ok,fills:[]}`.
Lobby share (`dasha-lobby-share.test.mjs`): `/lobby` OG names the room + Chat/Play/Fill the jar/Buy; `/forum` 308 `/lobby`; no Studio/Compute on the card.
Digest share (`dasha-digest-share.test.mjs`): `/digest` OG `$dasha Tape`; live tick title in og:description when Worker has one, else `Tick. Room. Buy.`; `/digest.json` keeps `tick`.
Faucet share (`dasha-faucet-share.test.mjs`): `/faucet` OG `Fill the jar` / `Claim. Fill. Buy.`; no airdrop; empty tape stays honest.
Sitemap: `/forum` `/privacy` `/bag` `/which` `/crew` `/digest` `/compute` `/contribute` `/bounties`; never `/studio` `/dasha` `/desk` `/login` or `lobby?`. `/compute` and `/crew` are live at the URL, not first-paint.
Stab: `/chess` API host, www `/chess/me` JSON, `/privacy` 200, `/compute` 200 edge=compute, `/crew` 200 edge=crew, dest-by-path 308s (`/siwg` → login#grok), www `/oauth/x/start` 308 lobby, sitemap no `/dasha`.
SIWG leftover (`dasha-siwg-compute-leftover.test.mjs`): `/siwg` GET+HEAD 308 `login#grok` (same as `/grok`); compute tab leftovers GET+HEAD 308 `/compute` no hash; `/compute` 200; `/compute/api` JSON; `/compute/api/healthz` 200; sitemap omits leftover 308s; no plugin.jup.ag.
Home tape (`dasha-digest-home.test.mjs`): `#dasha-digest` after `#grwm` / `#grok-door`; first paint has no tape section; HEAD remount `dasha-digest-remount` + `/digest.json` + `pack.tick` row 1 + quiet `/crew` line; `/digest` 200 full list; empty items no-op.
Live tick (`dasha-digest-tick.test.mjs`): Worker Dexscreener pair/token tick; remount never hits Dexscreener; failed tick keeps seed.
Canary contract (`dasha-canary-contract.test.mjs`): rollback fixture (home chess-door, privacy 308, dasha/desk dest `/`, graph 404, oauth 404, chess/me HTML, sitemap `/dasha`) fails; ship-src passes. SIWG stays.
Bag record (`dasha-bag-record.test.mjs`): hers skips Ansem; VVAIFU is not this; on-record fixture; neither 404; bad mint 400; timeout unknown. Leftover health facts stay.
Bag share (`dasha-bag-share.test.mjs`): `/bag?mint=` hers OG + jup.ag/tokens; VVAIFU points at `/which` no Buy; default `/bag` OG `$dasha · hers` / `Buy $dasha.` Health stays on the page.
Which OG (`dasha-which-og.test.mjs`): `/which` card `Which $dasha? dash_eats` / `dash_eats. Buy $dasha.`; page still names VVAIFU; no mint-match lecture.
Howto OG (`dasha-howto-og.test.mjs`): `/how-to-buy` card `How to buy $dasha` / `Buy $dasha.`; no mint lecture; no never-opens-a-wallet; jup.ag + mint only.
Howto leftover disclaimer (`dasha-howto-route-leftover.test.mjs`): drop leftover `Review the route there before confirming.` after style/script strip; Buy on Jupiter + mint stay; no plugin.jup.ag.
Howto leftover when-lecture (`dasha-howto-when-leftover.test.mjs`): drop leftover `Read from the Solana mint account on 18 August 2026 at finalized commitment.` after style/script strip; On-chain facts + Buy on Jupiter + mint stay; no plugin.jup.ag.
Howto leftover dropped-selector CSS (`dasha-howto-dropped-selector-css.test.mjs`): drop leftover `.risk` / `.facts .when` / `.facts .fine` CSS from live `/how-to-buy` `<style>` after those nodes were DOM-stripped (humans see it without style/script strip); skip-link + `.actions` stay; On-chain facts + Buy on Jupiter + mint stay; Watch price/ticker belt stays; no plugin.jup.ag.
Home leftover chrome (`dasha-home-chrome-leftover.test.mjs`): drop leftover `<nav class="dasha-nav">` and `<nav class="nav wrap">` from the document; leftover skip-link; leftover Webflow boot (comment/generator/favicon.ico/jquery/webflow.js) and leftover Studio/Desk CSS comment; leftover Webflow Commerce currency; leftover RETIRED product-mark / page-level-icon HTML comment; leftover cherries-lecture / view-transition-lecture / canonical-URL HTML comments; leftover Sign in menu / leftover hamburger `w-embed`; leftover product CSS Webflow wrapper unwrapped; no href=/simp in nav/footer; quiz `#simp-door` stays; grok-door stays; cherries SVG stays; `@view-transition` stays; product CSS + x-connect.js stay; `#compute-door` DOM-stripped; first paint `$dasha` + Chat + Buy; describedby; no px.gif.
Home leftover HTML comments (`dasha-home-comment-leftover.test.mjs`): drop leftover cherries-lecture / view-transition lecture / `Dasha canonical URL` comments after style/script strip; cherries SVG + `@view-transition` + canonical link stay; no plugin.jup.ag.
Home leftover 2020 portfolio CSS lecture (`dasha-home-portfolio-lecture.test.mjs`): drop leftover `Repairs for the 2020 portfolio template` / royal-blue / 1.1:1 WCAG lecture from live home `<style>` (humans see it without style/script strip); repair `#dasha-home h1/h2/label` rules stay; cherries SVG + `@view-transition` stay; no plugin.jup.ag.
Home leftover `.dasha-nav` hide CSS (`dasha-home-dasha-nav-hide-css.test.mjs`): drop leftover `.dasha-nav{display:none!important}` from live home repair `<style>` after nav DOM-strip (humans see it without style/script strip); repair `#dasha-home h1/h2/label` stay; Watch price/ticker belt stays; cherries SVG + `@view-transition` stay; no plugin.jup.ag.
Home leftover dropped-selector CSS (`dasha-home-dropped-selector-css.test.mjs`): drop leftover `.skip-link` / `footer` / `.compute` / `.poster` CSS from live home product `<style>` after those nodes were DOM-stripped (humans see it without style/script strip); repair `#dasha-home h1/h2/label` stay; Watch price/ticker belt stays; product skip-links stay; cherries SVG + `@view-transition` stay; no plugin.jup.ag.
Home leftover wrap-nav `.navlinks` CSS (`dasha-home-navlinks-leftover.test.mjs`): drop leftover `.navlinks` CSS from live home product `<style>` after wrap-nav DOM-strip (humans see it without style/script strip); mixed `@media` keeps `.dasha-hero` / `.pill` / `.contract`; do not strip `.pill`; repair `#dasha-home h1/h2/label` stay; Watch price/ticker belt stays; product skip-links stay; cherries SVG + `@view-transition` stay; no plugin.jup.ag.
Home leftover wrap-nav `.navlinks` CSS (`dasha-home-navlinks-leftover.test.mjs`): leftover `.navlinks` CSS dropped from live home product `<style>` after wrap-nav DOM-strip; mixed `@media` keeps `.dasha-hero` / `.pill` / `.contract`; repair `#dasha-home h1/h2/label` stay; Watch price/ticker belt stays; product skip-links stay; cherries SVG + `@view-transition` stay; no plugin.jup.ag.
Home leftover wrap-nav `.nav` / `.brand` / `.login-link` CSS (`dasha-home-wrap-nav-css.test.mjs`): drop leftover wrap-nav `.nav` / `.brand` / `.login-link` CSS from live home product `<style>` after wrap-nav DOM-strip (humans see it without style/script strip); mixed `@media` keeps `.dasha-hero` / `.pill` / `.contract`; do not strip `.pill`; repair `#dasha-home h1/h2/label` stay; Watch price/ticker belt stays; product skip-links stay; cherries SVG + `@view-transition` stay; no plugin.jup.ag.
Home leftover hero/door CSS (`dasha-home-hero-door-css.test.mjs`): drop leftover `.mint-lede` / `.actions` / `.door-actions` / `.door .copy-link` CSS from live home product `<style>` after those nodes were DOM-stripped (humans see it without style/script strip); mixed `@media` keeps `.dasha-hero` / `.pill` / `.contract`; do not strip `.pill`; keep `.copy` (contract); repair `#dasha-home h1/h2/label` stay; Watch price/ticker belt stays; product skip-links stay; cherries SVG + `@view-transition` stay; no plugin.jup.ag.
Home leftover `.lobby-log` CSS (`dasha-home-lobby-log-css.test.mjs`): drop leftover `.lobby-log` CSS from live home `#dasha-mobile-scroll` after the home DOM never had `class="lobby-log"` (humans see it in view-source); GRWM stays; lobby `.lobby-log` stays; Watch price/ticker belt stays; product skip-links stay; do not strip `.pill`; cherries SVG + `@view-transition` stay; no plugin.jup.ag.
Home leftover `#dasha-chess iframe` CSS (`dasha-home-chess-iframe-css.test.mjs`): drop leftover `#dasha-chess iframe` CSS from live home `#dasha-mobile-scroll` after the home DOM never had `#dasha-chess` (humans see it in view-source); GRWM stays; lobby `#dasha-chess` stays; Watch price/ticker belt stays; product skip-links stay; do not strip `.pill`; cherries SVG + `@view-transition` stay; no plugin.jup.ag.
Home leftover style id `dasha-home-play` (`dasha-home-play-style-id.test.mjs`): drop leftover `id="dasha-home-play"` from live home after Play was never on home (humans see it in view-source); faucet CSS + `#dasha-home-faucet` stay; GRWM stays; lobby `#dasha-chess` stays; Watch price/ticker belt stays; product skip-links stay; do not strip `.pill`; cherries SVG + `@view-transition` stay; no plugin.jup.ag.
Home leftover lang boot JS (`dasha-home-lang-script-leftover.test.mjs`): drop leftover `document.documentElement.lang ||= 'en'` from live home after `ensureHtmlLang` already sets `<html lang="en">` (humans see it in view-source); mint COPY + `DashaHomeMint` stay; GRWM stays; Watch price/ticker belt stays; product skip-links stay; do not strip `.pill`; cherries SVG + `@view-transition` stay; no plugin.jup.ag.
Home leftover Webflow.push JS (`dasha-home-webflow-push-leftover.test.mjs`): drop leftover `window.Webflow.push` from live home digest remount after webflow.js was already DOM-stripped (humans see it in view-source); remount + `/digest.json` stay; mint COPY + `DashaHomeMint` stay; GRWM stays; Watch price/ticker belt stays; product skip-links stay; do not strip `.pill`; cherries SVG + `@view-transition` stay; no plugin.jup.ag.
Home leftover `#simp` hash redirect JS (`dasha-home-simp-hash-leftover.test.mjs`): drop leftover `location.hash==='#simp'` / `q.has('quiz')` / `q.has('challenge')` client redirect from live home after `#simp-door` already paints and edge `/quiz` 308 + `/?challenge` 308 already fire (humans see it in view-source); quiz `#simp-door` stays; mint COPY + `DashaHomeMint` stay; GRWM stays; Watch price/ticker belt stays; product skip-links stay; do not strip `.pill`; cherries SVG + `@view-transition` stay; no plugin.jup.ag.
Privacy leftover dropped-selector CSS (`dasha-privacy-cta-leftover.test.mjs`): drop leftover `.cta` CSS from live `/privacy` `<style>` after the CTA node was already DOM-stripped (humans see it without style/script strip); product skip-link stays; Contribute `.cta` stays; Watch price/ticker belt stays; no plugin.jup.ag.
Bounties leftover dropped-selector CSS (`dasha-bounties-cta-leftover.test.mjs`): drop leftover `.cta` CSS from live empty `/bounties` `<style>` after the CTA node was already DOM-stripped (humans see it without style/script strip); funded listings keep `.cta`; product skip-link stays; Contribute `.cta` stays; Watch price/ticker belt stays; no plugin.jup.ag.
Worker html-404 leftover dropped-selector CSS (`dasha-notfound-cta-leftover.test.mjs`): drop leftover `.cta` CSS from live Worker 404 `<style>` after the 404 DOM never had `class="cta"` (humans see it without style/script strip); product skip-link stays; Contribute `.cta` stays; privacy/empty-bounties leftover `.cta` CSS stays dropped; Watch price/ticker belt stays; no plugin.jup.ag.
Leftover product footer (`dasha-product-footer-leftover.test.mjs`): drop Studio/Desk/Verse/Learn/Forum from footers; collapse leftover `·` holes; chess footer has no Forum word; how-to-buy footer keeps Privacy; quiz `#simp-door` stays; describedby; no plugin.jup.ag; `/forum` 308 `/lobby`.
Chess leftover Jupiter plugin mount (`dasha-chess-jup-plugin-leftover.test.mjs`): drop empty `#dasha-jup` from `/chess` after style/script strip; buy sheet + jup.ag + mint stay; chess-local stays; no plugin.jup.ag.
Chess leftover Jupiter plugin boot (`dasha-chess-jup-boot-leftover.test.mjs`): drop leftover `hideJup` / `bootJup` / `window.Jupiter.init` JS from live `/chess` after empty `#dasha-jup` was DOM-stripped (humans see it in view-source); buy sheet + jup.ag + `function jup()` + mint stay; chess-local stays; Watch price/ticker belt stays; no plugin.jup.ag.
Chess leftover Jupiter plugin boot call (`dasha-chess-jup-boot-call-leftover.test.mjs`): drop leftover `bootJup()` call from live `/chess` buy-chip handler after plugin boot functions were already dropped (humans see it in view-source); buy sheet + jup.ag + `function jup()` + mint stay; chess-local stays; Watch price/ticker belt stays; no plugin.jup.ag.
Chess leftover home mobile-scroll (`dasha-chess-mobile-scroll-leftover.test.mjs`): drop leftover `#dasha-mobile-scroll` from live `/chess` after polish always injected HOME GRWM CSS (humans see `#grwm` / `#dasha-home` in view-source); home mobile-scroll + GRWM stay; buy sheet + jup.ag + mint + chess-local stay; Watch price/ticker belt stays; no plugin.jup.ag.
Chess leftover `.dasha-quiet` dropped-selector CSS (`dasha-chess-dasha-quiet-css.test.mjs`): drop leftover `.dasha-slim a.dasha-quiet` CSS from live `/chess` `<style>` after that class was never in the chess DOM (slim header is `.dasha-word` + `.buy-dasha`; humans see it without style/script strip); keep `.dasha-slim` / `.dasha-word` / `.buy-dasha`; keep `.empty` + `.identity`; buy sheet + jup.ag + mint + chess-local stay; Watch price/ticker belt stays; no plugin.jup.ag.
Chess leftover `.panel` dropped-selector CSS (`dasha-chess-panel-css.test.mjs`): drop leftover `.panel` CSS from live `/chess` `<style>` after that class was never in the chess DOM (shell is `.app` + `.gate`; humans see it without style/script strip); keep `.app` / `.gate`; keep `.dasha-slim` / `.dasha-word` / `.buy-dasha`; keep `.empty` + `.identity`; buy sheet + jup.ag + mint + chess-local stay; Watch price/ticker belt stays; no plugin.jup.ag.
Chess leftover Play chrome id (`dasha-chess-gate-actions-id-leftover.test.mjs`): drop leftover `id="gate-actions"` from live `/chess` after CSS/JS strip (JS never reads `getElementById('gate-actions')`; CSS never targets `#gate-actions`; keep `class="gate-actions"` + `#gate-action`; keep `.app` / `.gate`; humans see it in view-source); buy sheet + jup.ag + mint + chess-local stay; Watch price/ticker belt stays; no plugin.jup.ag.
Chess leftover buy-share TG id (`dasha-chess-buy-share-tg-id-leftover.test.mjs`): drop leftover `id="buy-share-tg"` from live `/chess` after CSS/JS strip (JS never reads `getElementById('buy-share-tg')`; CSS never targets `#buy-share-tg`; keep the TG href + `#buy-share-x`; keep `#buy-sheet`; humans see it in view-source); buy sheet + jup.ag + mint + chess-local stay; Watch price/ticker belt stays; no plugin.jup.ag.
Chess leftover buy-sheet fallback id (`dasha-chess-buy-sheet-fallback-id-leftover.test.mjs`): drop leftover `id="buy-sheet-fallback"` from live `/chess` after CSS/JS strip (JS never reads `getElementById('buy-sheet-fallback')`; CSS never targets `#buy-sheet-fallback`; keep `#buy-mint` + `#buy-sheet` + TG href + `#buy-share-x`; humans see it in view-source); buy sheet + jup.ag + mint + chess-local stay; Watch price/ticker belt stays; no plugin.jup.ag.
Chess leftover Top table id (`dasha-chess-leaders-panel-id-leftover.test.mjs`): drop leftover `id="leaders-panel"` from live `/chess` after CSS/JS strip (JS never reads `getElementById('leaders-panel')`; CSS never targets `#leaders-panel`; keep `class="leaders"` + `#leaders`; humans see it in view-source); buy sheet + jup.ag + mint + chess-local stay; Watch price/ticker belt stays; no plugin.jup.ag.
Chess leftover watch:true JS comment (`dasha-chess-watch-true-comment-leftover.test.mjs`): drop leftover `watch:true` JS comment from live `/chess` after `watchingGame()` already inlines `g.watch===true` (humans see it in view-source); keep `watchingGame()`; leftover Invite / 1v1 comment is a separate leftover; buy sheet + jup.ag + mint + chess-local stay; Watch price/ticker belt stays; no plugin.jup.ag.
Chess leftover Invite / 1v1 JS comment (`dasha-chess-invite-1v1-comment-leftover.test.mjs`): drop leftover `Invite / 1v1` JS comment from live `/chess` after `challengeShareUrl` (humans see it in view-source); not live UI copy; keep `#gate-invite` + `textContent='Invite'` + JSON-LD Play. Invite. Find.; keep `watchingGame()` + `g.watch===true`; buy sheet + jup.ag + mint + chess-local stay; Watch price/ticker belt stays; no plugin.jup.ag.
Chess leftover showLecture JS (`dasha-chess-show-lecture-leftover.test.mjs`): drop leftover `function showLecture(title,copy){...}` from live `/chess` after `hideLecture()` already hides `#gate-kicker`/`#gate-title`/`#gate-copy` (never called; humans see it in view-source); keep `hideLecture()` + gate copy ids; keep `#gate-invite` + Invite copy + JSON-LD Play. Invite. Find.; keep `watchingGame()` + `g.watch===true`; buy sheet + jup.ag + mint + chess-local stay; Watch price/ticker belt stays; no plugin.jup.ag.
Chess leftover unused JS (`dasha-chess-unused-js-leftover.test.mjs`): drop leftover `function loadLeaders` / `function playNow` / `function flashBought` from live `/chess` after never called (no onclick / string refs; humans see them in view-source); keep `#leaders` + `class="leaders"` + `#gate-action` + `findNow` + `joinQueue`; keep `hideLecture()` + Invite copy + `watchingGame()`; buy sheet + jup.ag + mint + chess-local stay; Watch price/ticker belt stays; no plugin.jup.ag.
Chess leftover recent games id (`dasha-chess-recent-panel-id-leftover.test.mjs`): drop leftover `id="recent-panel"` from live `/chess` after leftover unused JS `loadLeaders` was already stripped (JS never reads `getElementById('recent-panel')`; CSS never targets `#recent-panel`; keep `class="recent"` + `#recent` + `class="leaders"` + `#leaders`; humans see it in view-source); keep `tournamentAction(action,name)` + `tournamentAction('create'`; buy sheet + jup.ag + mint + chess-local stay; Watch price/ticker belt stays; no plugin.jup.ag.
Chess leftover promotion title id (`dasha-chess-promotion-title-id-leftover.test.mjs`): drop leftover `id="promotion-title"` from live `/chess` after CSS/JS strip (JS never reads `getElementById('promotion-title')`; CSS never targets `#promotion-title`; keep `class="promotion"` + `#promotion` + `.promotion p` + Promote to copy; dangling `aria-labelledby="promotion-title"` drops with the id; keep `id="tc-3"` / `id="tc-5"` / `id="tc-10"` because JS binds `$('tc-'+n)`; keep `class="recent"` + `#recent`; humans see it in view-source); keep `tournamentAction(action,name)` + `tournamentAction('create'`; buy sheet + jup.ag + mint + chess-local stay; Watch price/ticker belt stays; no plugin.jup.ag.
Chess leftover unused casual-play JS (`dasha-chess-casual-play-js-leftover.test.mjs`): drop leftover `function casualRematch` / `function nextPlay` / `function playReady` / `function showPlayPair` from live `/chess` after never called (no onclick / string refs; humans see them in view-source); keep `showCasualBar()` + `hidePlayPair()` + `#gate-find` + `#gate-action`; keep `hideLecture()` + Invite copy + `watchingGame()`; buy sheet + jup.ag + mint + chess-local stay; Watch price/ticker belt stays; no plugin.jup.ag.
Chess leftover unused tournament chrome CSS (`dasha-chess-tournament-chrome-css-leftover.test.mjs`): drop leftover `.tournament-meta` / `.bracket` / `.champion` / `.entrants` / `.tournament-actions` CSS from live `/chess` `<style>` after those classes were never in the chess DOM (`wantTournamentChrome()` is false; static markup is `#tournament` + `.tournament-form`; humans see leftover tournament essay CSS in view-source); keep `#tournament` + `.tournament-form`; keep `showCasualBar()` + `hidePlayPair()` + `#gate-find` + `#gate-action`; keep `hideLecture()` + Invite copy + `watchingGame()`; buy sheet + jup.ag + mint + chess-local stay; Watch price/ticker belt stays; no plugin.jup.ag.
Chess leftover unused tournament chrome JS classNames (`dasha-chess-tournament-chrome-js-leftover.test.mjs`): drop leftover `'tournament-meta'` / `'tournament-actions'` / `'entrants'` / `'bracket'` / `'champion'` JS className strings from live `/chess` after CSS drop (`wantTournamentChrome()` is false; static markup is `#tournament` + `.tournament-form`; `renderTournament` / `renderChallenge` still run for the hidden form path so they stay; leftover className cluster never paints; humans see leftover tournament className strings in view-source); keep `#tournament` + `.tournament-form` + `renderTournament()` + `renderChallenge()`; keep `showCasualBar()` + `hidePlayPair()` + `#gate-find` + `#gate-action`; keep `hideLecture()` + Invite copy + `watchingGame()`; buy sheet + jup.ag + mint + chess-local stay; Watch price/ticker belt stays; no plugin.jup.ag. Do not restore leftover CSS rules.
Lobby leftover Full table dropped-selector CSS (`dasha-lobby-play-full-css.test.mjs`): drop leftover `.forum-play-full` CSS from live `/lobby` `<style>` after Full table was DOM-stripped (humans see it without style/script strip); `#forum-play-go` / `#dasha-forum` / `#dasha-chess` / `/forum/tape` stay; Watch price/ticker belt stays; no plugin.jup.ag.
Lobby leftover mint tape (`dasha-lobby-mint-tape-leftover.test.mjs`): drop empty CSS-hidden `#dasha-mint-tape` from `/lobby` after style/script strip; `#dasha-forum` / `#forum-play-go` / `/forum/tape` stay; `#dasha-digest` stays; no plugin.jup.ag.
Lobby leftover home digest remount (`dasha-lobby-digest-remount-leftover.test.mjs`): drop leftover `#dasha-digest-remount` from live `/lobby` after remount boot is home-only (humans see it in view-source); home remount + `/digest.json` stay; `#dasha-forum` / `#forum-play-go` / `#dasha-chess` stay; Watch price/ticker belt stays; no plugin.jup.ag.
Lobby leftover home mobile-scroll (`dasha-lobby-mobile-scroll-leftover.test.mjs`): drop leftover `#dasha-mobile-scroll` from live `/lobby` after polish always injected HOME GRWM CSS (humans see `#grwm` / `#dasha-home` in view-source); home mobile-scroll + GRWM stay; `#dasha-forum` / `#forum-play-go` / `#dasha-chess` stay; Watch price/ticker belt stays; no plugin.jup.ag.
Lobby leftover `.forum-list` dropped-selector CSS (`dasha-lobby-forum-list-css.test.mjs`): drop leftover `.forum-list,.forum-thread` CSS from live `/lobby` `<style>` after those classes were never in the lobby DOM (threads mount is `#dasha-forum` + `df-*`; humans see it without style/script strip); `#forum-play-go` / `#dasha-forum` / `#dasha-chess` / `.lobby-log` / `.forum-threads` stay; Watch price/ticker belt stays; no plugin.jup.ag.
Lobby leftover `.forum-title` dropped-selector CSS (`dasha-lobby-forum-title-css.test.mjs`): drop leftover `.forum-title` CSS from live `/lobby` `<style>` after that class was never in the lobby DOM (threads mount is `#dasha-forum` + `df-title`; humans see it without style/script strip); `#forum-play-go` / `#dasha-forum` / `#dasha-chess` / `.lobby-log` / `.forum-threads` stay; Watch price/ticker belt stays; no plugin.jup.ag.
Lobby leftover `.forum-row` dropped-selector CSS (`dasha-lobby-forum-row-css.test.mjs`): drop leftover `.forum-row` CSS from live `/lobby` `<style>` after that class was never in the lobby DOM (threads mount is `#dasha-forum` + `df-row`; humans see it without style/script strip); `#forum-play-go` / `#dasha-forum` / `#dasha-chess` / `.lobby-log` / `.forum-threads` stay; Watch price/ticker belt stays; no plugin.jup.ag.
Lobby leftover `.forum-replies,.forum-when` dropped-selector CSS (`dasha-lobby-forum-replies-css.test.mjs`): drop leftover `.forum-replies,.forum-when` CSS from live `/lobby` `<style>` after those classes were never in the lobby DOM (threads mount is `#dasha-forum` + `df-*`; humans see it without style/script strip); `#forum-play-go` / `#dasha-forum` / `#dasha-chess` / `.lobby-log` / `.forum-threads` stay; Watch price/ticker belt stays; no plugin.jup.ag.
Lobby leftover `.forum-post,.forum-reply` dropped-selector CSS (`dasha-lobby-forum-post-css.test.mjs`): drop leftover `.forum-post,.forum-reply` CSS prefixes from live `/lobby` `<style>` after those classes were never in the lobby DOM (threads mount is `#dasha-forum` + `df-*`; keep `.lobby-line`; humans see it without style/script strip); `#forum-play-go` / `#dasha-forum` / `#dasha-chess` / `.lobby-log` / `.forum-threads` stay; Watch price/ticker belt stays; no plugin.jup.ag.
Lobby leftover `.forum-meta` dropped-selector CSS (`dasha-lobby-forum-meta-css.test.mjs`): drop leftover `.forum-meta` CSS prefix from live `/lobby` `<style>` after that class was never in the lobby DOM (threads mount is `#dasha-forum` + `df-meta`; keep `.lobby-meta`; humans see it without style/script strip); `#forum-play-go` / `#dasha-forum` / `#dasha-chess` / `.lobby-log` / `.forum-threads` stay; Watch price/ticker belt stays; no plugin.jup.ag.
Lobby leftover `.dasha-forum` dropped-selector CSS (`dasha-lobby-dasha-forum-css.test.mjs`): drop leftover `.dasha-forum` CSS from live `/lobby` `<style>` after that class was never in the lobby DOM (threads mount is `#dasha-forum` + `df-*`; humans see it without style/script strip); `#forum-play-go` / `#dasha-forum` / `#dasha-chess` / `.lobby-log` / `.forum-threads` stay; keep `.dasha-lobby`; Watch price/ticker belt stays; no plugin.jup.ag.
Lobby leftover `.dasha-quiet` dropped-selector CSS (`dasha-lobby-dasha-quiet-css.test.mjs`): drop leftover `.dasha-slim a.dasha-quiet` CSS from live `/lobby` `<style>` after that class was never in the lobby DOM (slim header is `.dasha-word` + `.buy-dasha`; humans see it without style/script strip); keep `.dasha-slim` / `.dasha-word` / `.buy-dasha`; keep `.dasha-lobby`; keep `#dasha-forum`; `#forum-play-go` / `#dasha-chess` / `.lobby-log` / `.forum-threads` stay; Watch price/ticker belt stays; no plugin.jup.ag.
Lobby leftover Play id (`dasha-lobby-forum-play-id-leftover.test.mjs`): drop leftover `id="forum-play"` from live `/lobby` after CSS/JS strip (JS never reads `getElementById('forum-play')`; CSS never targets `#forum-play`; keep `class="forum-play"` + `#forum-play-go` + `.forum-send`; humans see it in view-source); `#dasha-forum` / `#dasha-chess` / `.lobby-log` / `.forum-threads` stay; keep `.dasha-lobby`; Watch price/ticker belt stays; no plugin.jup.ag.
Lobby leftover `.forum-form` dropped-selector CSS (`dasha-lobby-forum-form-css-leftover.test.mjs`): drop leftover mixed `.forum-form` CSS prefixes from live `/lobby` `<style>` after that class was never in the lobby DOM (composer is `.lobby-form`; humans see it without style/script strip); keep `.lobby-form` + `.lobby-form input/textarea` + `:focus` + `.lobby-nick[hidden]` + `.lobby-xbar[hidden]`; leftover mixed `.forum-body` / `.forum-status` dropped by later leftover; `#forum-play-go` / `#dasha-forum` / `.dasha-lobby` / `.forum-send` stay; Watch price/ticker belt stays; no plugin.jup.ag.
Lobby leftover `.forum-body` / `.forum-status` dropped-selector CSS (`dasha-lobby-forum-body-css-leftover.test.mjs`): drop leftover mixed `.forum-body` / `.forum-status` CSS prefixes from live `/lobby` `<style>` after those classes were never in the lobby DOM (lobby.js mounts `lobby-body` / `lobby-status`; threads are `#dasha-forum` + `df-body` / `df-status`; humans see it without style/script strip); keep `.lobby-body` + `.lobby-body a` + `.lobby-status` + `.lobby-status[data-kind=bad]`; keep `.lobby-form`; `#forum-play-go` / `#dasha-forum` / `.dasha-lobby` / `.forum-send` stay; Watch price/ticker belt stays; no plugin.jup.ag.
Robots leftover lecture (`dasha-robots-lecture-leftover.test.mjs`): drop leftover Webflow SEO / 2026-08-08 outage / 2020 e-commerce / Disallow-history / CC0 essay comments from `/robots.txt`; User-agent / Allow / Sitemap + identity one-liner stay; no plugin.jup.ag.
Compute leftover meta (`dasha-compute-desc-leftover.test.mjs`): drop leftover `Sponsor a Mac for the getdasha.com compute network` from `/compute` meta after style/script strip; OG `Use. Provide. Night. Build. Sponsor.` + Mac Studio + Use/Provide/Night/Build/Sponsor stay; no plugin.jup.ag.
Compute leftover facts `checking` placeholders (`dasha-compute-facts-checking-placeholder-leftover.test.mjs`): drop leftover `checking` on `#destination` / `#retention` / `#visibility` after `updateRun()` overwrites immediately with no probe (humans see it in view-source); keep ids + hosted defaults + `#gateway-state` / `#provider-count` / `#top-state` checking + Checking…; Use/Provide/Night/Build stay; no plugin.jup.ag.
Compute leftover empty `#request-json` / `#code` (`dasha-compute-empty-request-json-code-leftover.test.mjs`): drop leftover empty `#request-json` / `#code` after `paintRequest()` / `paintCode()` overwrite immediately from form defaults with no probe (humans see empty tags in view-source); keep ids + `#code-curl` + hosted Checking…; Use/Provide/Night/Build stay; no plugin.jup.ag.
Compute leftover stale `#count` (`dasha-compute-stale-count-leftover.test.mjs`): drop leftover stale `#count` `0 characters · nothing sent` after `paintRequest()` overwrite immediately from form defaults with no probe (humans see it in view-source); keep id + hosted Checking…; Use/Provide/Night/Build stay; no plugin.jup.ag.
Compute leftover stale `#route-note` (`dasha-compute-stale-route-note-leftover.test.mjs`): drop leftover stale `#route-note` `Hosted: GPT-OSS 20B · Community: prompts are visible to the Mac operator.` after `updateRun()` overwrite immediately from hosted engine default with no probe (humans see it in view-source); keep id + community-operator product copy in JS + hosted Checking…; Use/Provide/Night/Build stay; no plugin.jup.ag.
Compute leftover empty `#model` (`dasha-compute-empty-model-select-leftover.test.mjs`): drop leftover empty `#model` after `MODELS.forEach` overwrite immediately from disk models with no probe (humans see empty select in view-source); keep id + empty `#chip` / `#ram` / `#night-model` leftovers + hosted Checking…; Use/Provide/Night/Build stay; no plugin.jup.ag.
OpenAI leftover completions/responses/GET-chat (`dasha-compute-v1-completions-responses.test.mjs`): POST `/compute/api/v1/completions` + `/responses` (slash) unauth 401 OpenAI JSON, authed 400 not supported pointing at POST `/v1/chat/completions`; GET `/v1/chat/completions` 401/405 JSON not `{error:not found}`; POST chat 503 empty net; embeddings POST 401; models list/retrieve 401; foo 404; no ghost jobs / fake Mac; no plugin.jup.ag.
Home leftover Webflow `dasha-root` class (`dasha-home-dasha-root-class-leftover.test.mjs`): drop leftover `class="dasha-root"` from live home `#dasha-home` after product `#dasha-home` + `.dasha` CSS (johns-awesome `.dasha-root{min-height:100vh}` still paints after style/script strip); product `#dasha-home` + johns-awesome stay; mint COPY + `DashaHomeMint` stay; GRWM stays; Watch price/ticker belt stays; product skip-links stay; do not strip `.pill`; cherries SVG + `@view-transition` stay; no plugin.jup.ag.
Home leftover `dasha-home-lede` class (`dasha-home-dasha-home-lede-class-leftover.test.mjs`): drop leftover `class="dasha-home-lede"` from live home after CSS/JS strip (CSS never targets `.dasha-home-lede`; lede paints via `#dasha-home-lede`; JS never reads `querySelector('.dasha-home-lede')`; humans see it in view-source); keep `#dasha-home-lede` + culture/mint line; keep `#chat-door` / `#simp-door`; Watch price/ticker belt stays; no plugin.jup.ag.
Home leftover `buy-dasha` class (`dasha-home-buy-dasha-class-leftover.test.mjs`): drop leftover `class="buy-dasha"` from live home after CSS/JS strip (CSS never targets `.buy-dasha`; contract Buy paints via `.pill.primary`; JS never reads `querySelector('.buy-dasha')`; humans see it in view-source); keep `.pill.primary` + Buy $dasha ↗ + jup.ag + `#mint` + `.copy`; lobby/chess/simp `.buy-dasha` stay; Watch price/ticker belt stays; no plugin.jup.ag.
Bounties leftover X-connect (`dasha-bounties-x-connect-leftover.test.mjs`): live `/bounties` injected `x-connect.js` only; site-hunt needs `oauth/x` or `id=bb-x` / `#bb-x`. Quiet optional Connect X `#bb-x` → `/oauth/x/start?continue=1`. GitHub contribute stays required. No disclaimer lecture. Watch price/ticker belt stays; no plugin.jup.ag.

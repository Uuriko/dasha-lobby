# Compute gate-first cold start

Cold `/compute` first paint is the intent gate (**Start.**), not Ask.

1. **Gate** visible: Ask · Provide · Pay · Credits (Marketplace quiet on Ask only). Quiet **Sign in** (logged out) or **You**/`@handle` (logged in) under the four doors — not a fifth primary.
2. **Ask** hidden until pick; Hosted default after Ask (and after Pay → Top up / Credits → Use credits).
3. **Pay** → `#step-pay` (Top up → Amount → USDC/$dasha → Send · Sponsor → Amount → USDC/$dasha → Send). Stripe deferred. **Credits** → `#step-credits` (real balance when logged in · Use credits → Ask · Top up → Amount).
4. **You.** (`#you` / `#account`) — identity fine line + Macs · Credits · API · Log out. Back → gate. Log out → POST `/auth/logout` → gate.
5. **Ask starters** (≤3 quiet chips: Welcome note / Summarize this / Draft a curl) fill useful prompts; API/curl examples use `hello` — never “say something strange”.
6. **Back** on Ask shows when `cameFromGate` or `cameFromHow` (gate or How). Pay/Credits/You Back → gate.
7. Deep links: `#provide` `#build` `#market`/`#ocm` `#host` `#ask` `#you`/`#account` still work.
8. Provide Done → Ask (not gate). Honesty: gate never boasts Community when `providers_online:0`.

See also `COMPUTE-COMPETITOR-INSPIRE.md` N1–N5.
